#include <stdio.h>

int main()
{
    int x, y;

    printf("What's x? ");
    scanf("%i", &x);

    printf("What's y ? ");
    scanf("%i", &y);

    //int z = x + y;

    printf("The sum equals %i\n", x + y);
}